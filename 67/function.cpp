
#include "SomeFunctions.hpp"
#include <iostream>
#include <string>

using namespace std;
int main()
{
    int n, m;
    int matrix[N][M];
    Read(n, m, matrix);
    max(n, m, matrix);
    sort(n, m, matrix);
    Write(n, m, matrix);
}

